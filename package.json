{
  "name": "electron-compile-test",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "electron": "electron .",
    "build": "NODE_ENV='production' electron-packager-compile . --out=dist --app-version=$npm_package_version --version=1.3.2 --asar --prune --overwrite --platform=darwin --arch=x64"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron-compilers": "^3.1.6",
    "electron-packager": "^7.6.0",
    "electron-prebuilt-compile": "^1.3.2"
  },
  "dependencies": {
    "electron-compile": "^3.3.5"
  }
}
